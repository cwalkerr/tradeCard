<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign Up</title>
    <link rel="stylesheet" href="/css/custom.css" />
  </head>
  <body>
    <!-- BG IMAGE revisit issue 100vh not working on iphone, works if i take it off but then doesnt display on desktop correctly-->
    <div
      class="container-fluid"
      style="
        background-image: url(/media/flat-mountains.svg);
        background-size: cover;
        height: 100vh;
        width: 100%;
      "
    >
      <!-- MODAL FOR ERROR MESSAGES -->
      <div
        class="modal fade"
        id="modal"
        tabindex="-1"
        aria-labelledby="exampleModalLabel"
        aria-hidden="true"
      >
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h1 class="modal-title fs-5" id="">Modal title</h1>
              <button
                type="button"
                class="btn-close"
                data-bs-dismiss="modal"
                aria-label="Close"
              ></button>
            </div>
            <div class="modal-body" id="modal-body"></div>
            <div class="modal-footer">
              <button
                type="button"
                class="btn btn-secondary"
                data-bs-dismiss="modal"
              >
                Close
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- LOGO -->
      <div class="container d-flex justify-content-center">
        <a href="/index.html">
          <img
            class="mt-5"
            src="/media/logowhite.png"
            alt="logo"
            height="200px"
            width="200px"
          />
        </a>
      </div>

      <!-- TEXT & LOG IN REDIRECT-->
      <div class="container mt-3">
        <p class="display-6 text-center text-white mb-3">Create Account</p>
        <div class="my-4 text-center text-white">
          <p class="lead">
            Already have an account?
            <a class="text-warning" href="/login">Sign in here</a>
          </p>
        </div>

        <% if (error) { %>
        <p class="lead text-center text-white mb-3"><%=error%></p>
        <% } %>

        <!-- FORM review accessability-->

        <form action="/signup" method="POST">
          <div class="row mb-3 justify-content-center">
            <!-- revisit responsive sizes -->
            <div class="col-sm-9 col-md-7 col-lg-5">
              <label for="userEmail" class="form-label lead text-white"
                >Email address</label
              >
              <input
                type="email"
                class="form-control"
                id="userEmail"
                name="email"
                placeholder="name@example.com"
              />

              <p class="text-white text-center text-opacity-50 mt-1">
                We will not send you any marketing emails.
              </p>
            </div>
          </div>

          <div class="row mb-3 justify-content-center">
            <!-- revisit responsive sizes -->
            <div class="col-sm-9 col-md-7 col-lg-5">
              <label for="username" class="form-label lead text-white"
                >Username</label
              >
              <input
                type="text"
                class="form-control"
                id="username"
                name="username"
                placeholder="username"
              />

              <p class="text-white text-center text-opacity-50 mt-1">
                Can include letters, numbers, and underscores. No special
                characters.
              </p>
            </div>
          </div>

          <div class="row mb-3 justify-content-center">
            <div class="col-sm-9 col-md-7 col-lg-5">
              <label for="userPassword" class="form-label lead text-white"
                >Password</label
              >
              <input
                type="password"
                class="form-control"
                id="userPassword"
                name="password"
                placeholder="must be 8-20 characters long"
              />
            </div>
          </div>

          <div class="row mb-3 justify-content-center">
            <div class="col-sm-9 col-md-7 col-lg-5">
              <label for="userPasswordCheck" class="form-label lead text-white"
                >Re-Enter Password</label
              >
              <input
                type="password"
                class="form-control"
                id="userPasswordCheck"
                name="passwordCheck"
                placeholder="enter password again"
              />
            </div>
          </div>
          <div class="d-flex justify-content-center mt-5">
            <button type="submit" class="btn btn-success btn-lg">
              Create Account
            </button>
          </div>
        </form>
      </div>
    </div>

    <script src="../node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
